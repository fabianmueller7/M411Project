#include<stdio.h>
#include<stdlib.h>

typedef struct Elm {
    int Nr;
    struct Elm* pNext; 
} struElm;

for (intiElm = 0; iElm < Anzahl; iElm++) {
    // Element erstellen und initialisieren
    pNew = (struElm*)malloc(sizeof(struElm));